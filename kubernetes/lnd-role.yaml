apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: lnd-secret-writer-role
  namespace: lightning-playground
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["lnd-credentials"]
  verbs: ["patch"]
